[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pytest-playwright = "*"

[dev-packages]

[requires]
python_version = "3.8"

[scripts]
install = "playwright install"
test = "pytest tests/"
headtest = "pytest --headed --browser firefox --slowmo 500 tests/"